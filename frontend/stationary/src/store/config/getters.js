
const name = state => {
    return state.name
}
const quickMode = state => {
  return state.quickMode
}
const displayedItems = state => {
  return state.displayedItems
}

export default {
  name,
  quickMode,
  displayedItems
}